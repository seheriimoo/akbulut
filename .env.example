# Local secrets for Flutter --dart-define-from-file
# Copy to secrets.local.json and fill real values.
# secrets.local.json is gitignored and must never be committed.

OPENAI_API_KEY=
REVENUECAT_API_KEY=
SENTRY_DSN=
